#include "Logger.h"
#include <iostream>

void Logger::logMessage(const std::string& message) {
    // Implementieren Sie die Logik zum Schreiben der Nachricht
    // Beispiel: Ausgabe auf der Konsole
    std::cout << message << std::endl;
}